# Generated by libFuzzer at run time. `seeds/` holds the hand-written starting
# inputs that are worth committing; everything the fuzzer derives from them is
# reproducible and can run to thousands of files, so it stays local.
corpus/
artifacts/
coverage/
target/
